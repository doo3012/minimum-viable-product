'use client';

import { AppShell } from '@/components/AppShell';

export default function BuManagementLayout({ children }: { children: React.ReactNode }) {
  return <AppShell>{children}</AppShell>;
}
